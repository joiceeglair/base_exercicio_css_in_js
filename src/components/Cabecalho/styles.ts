import styled from 'styled-components'

import { Cores } from '../../styles'

export const CabecalhorH = styled.header`
  background-color: ${Cores.corSecundaria};
  color: ${Cores.corPrincipal};
  text-align: center;
  padding: 24px 0;
`
